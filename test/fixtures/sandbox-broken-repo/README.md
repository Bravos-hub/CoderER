# CodeER sandbox failure fixture

This fixture intentionally exits with code `17`, emits a stable failure signature, writes one bounded artifact, and prints a synthetic credential-like value that must be redacted before persistence.
